import { Button, Paper, styled } from "@mui/material";

export const BasketTotalInner = styled(Paper)({
  padding: 24,
  maxWidth: "350px",
});

export const BasketBuyButton = styled(Button)({
  padding: "16px 24px",
  fontSize: 16,
  fontWeight: 700,
});
